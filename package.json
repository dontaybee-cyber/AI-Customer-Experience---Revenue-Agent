{
  "name": "ai-cx-revenue-agent",
  "private": true,
  "version": "0.1.0",
  "description": "AI Customer Experience & Revenue Agent monorepo (omnichannel shared memory + trigger engine + connectors).",
  "packageManager": "pnpm@9.15.5",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter @acx/api dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter @acx/api start",
    "check": "pnpm -r check",
    "lint": "oxlint src",
    "format:check": "oxfmt --check .",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "@typescript/native-preview": "7.0.0-dev.20260224.1",
    "oxfmt": "^0.35.0",
    "oxlint": "^1.50.0",
    "typescript": "^5.9.3"
  }
}
